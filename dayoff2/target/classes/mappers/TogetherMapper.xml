<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.project.mapper.togetherMapper">

	<insert id="togetherInsert" parameterType="togetherVo" useGeneratedKeys="true" keyProperty="tidx">
		INSERT INTO togethertable(
			 ttitle
			, tcontent
			, tdate
			, thit
			, thashtag
			, tfilename
			, uidx
			, writer
		)VALUES(
			 #{tTitle}
			, #{tContent}
			, now()
			, 0
			, '먹방'
			, 0
			, #{uidx}
			, #{writer}
		)
	</insert>
	
	<insert id="imageEnroll" parameterType="AttachImage2Vo">
		insert into image2table(
			tidx, fileName, uploadPath, uuid
			) values (
			#{tidx}, #{fileName}, #{uploadPath}, #{uuid}
			)
	</insert>
	
	<select id="image" resultType="AttachImage2Vo" parameterType="int">
	
		select
			tidx,
			fileName,
			uploadPath,
			uuid
		FROM image2table
		where tidx = #{tidx}
	</select>
	
	
	
	<select id="togetherlist" resultType="togetherVo">
	
    	SELECT *
    	FROM togethertable
    	order by tidx desc
    		
    </select>
	
	<select id="selectByTidx" parameterType="int" resultType="togetherVo">
    	SELECT tidx
    		 , ttitle
    		 , tcontent
    		 , tdate
    		 , thit
    		 , uidx
    		 , writer
    		FROM togethertable
    		WHERE tidx = #{tidx}	<!-- DAO에서 파라미터로 넘긴 변수명 -->
    </select>
    
    <update id="updateByTidx" parameterType="togetherVo" >
    	UPDATE togethertable
    	   SET ttitle = #{tTitle}
    		 , tcontent= #{tContent}
    	WHERE tidx = #{tidx} 
    </update>
    
    <delete id="deleteByTidx" parameterType="int">
    	DELETE 
    	FROM  togethertable
    	WHERE tidx = #{tidx} 
    </delete>
    
    
	
</mapper>