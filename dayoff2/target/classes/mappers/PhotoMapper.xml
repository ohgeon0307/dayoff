<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.project.mapper.photoMapper">
	<select id="list" resultType="photoVo" parameterType="photoVo">
		select *
		FROM phototable
	</select>
	
	<select id="selectByPidx" resultType="photoVo" parameterType="int">
		select pidx
			, pTitle
			, pDate
			, uidx
			, pDelYn
			, pHashTag
			, pHit
		FROM phototable
		WHERE pidx = #{pidx}
	</select>
	
	<select id="updateByPidx" parameterType="int" resultType="photoVo">
		SELECT pidx
		, pTitle
		, pHashTag
		, uidx
		, pDelYn
		, pHashTag
		, pHit
		, pWriter
		FROM phototable
		WHERE pidx = #{pidx}
	</select>
	
	<delete id="deleteByPidx" parameterType="int">
		DELETE
			FROM phototable
		WHERE pidx = #{pidx}
	</delete>
	
	<insert id="insert" parameterType="photoVo" useGeneratedKeys="true" keyProperty="pidx"> 	
		
		INSERT INTO phototable(
			 pTitle
			, pDate
			, uidx
			, pHashTag
			, pHit
		) VALUES(
			 #{pTitle}
			, NOW()
			, 1
			, #{pHashTag}
			, 0
		)
<!--  		<selectKey keyProperty="pidx" resultType="Integer" order="AFTER">
			SELECT LAST_INSERT_ID() as pidx
		</selectKey>
 -->	</insert>
	
		<!-- 이미지 등록 -->
	<insert id="imageEnroll" parameterType="AttachImageVo">
		insert into imagetable(
			pidx, fileName, uploadPath, uuid
			) values (
			#{pidx}, #{fileName}, #{uploadPath}, #{uuid}
			)
	</insert>
	
	<select id="imageList" resultType="AttachImageVo" parameterType="int">
	
		select
			pidx,
			fileName,
			uploadPath,
			uuid
		FROM imagetable
		where pidx = #{pidx}
	</select>
	
	<update id="modifyByBidx" parameterType="photoVo">
		UPDATE phototable
		SET
		pTitle
		= #{pTitle}
		, pHashTag = #{pHashTag}
		WHERE pidx
		= #{pidx}
		
	</update>
	
	
</mapper>