<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.project.mapper.boardMapper">
	<insert id="boardInsert" parameterType="boardVo">
		INSERT INTO boardtable(
		  bidx
		, title
		, content
		, wdate
		, hit
		, hashTag
		, filename
		, likes
		, uidx
		, bDelYn
		)VALUES(
		pbidx_seq.nextval
		, #{title}
		, #{content}
		, sysdate
		, 0
		, #{hashTag}
		, 'hi'
		, 0
		, 1
		, 'N'
		)
	</insert>
	
	<select id="boardList" resultType="boardVo" parameterType = "boardVo">
		SELECT bidx
		, title
		, content
		, hashTag
		, uidx
		, hit
		FROM boardtable
		ORDER BY bidx DESC
	</select>
	
	<select id = "selectByBidx" parameterType="int" resultType="boardVo">
		SELECT bidx
		, title
		, content
		, hashTag
		, uidx
		, hit
		FROM boardtable
		WHERE bidx = #{bidx}
	</select>
	
	<select id ="updateByBidx" parameterType = "int" resultType = "boardVo">
	SELECT bidx
	, title
	, content
	, hashTag
	, uidx
	, hit
	FROM boardtable
	WHERE bidx = #{bidx}
	</select>
	
	<update id = "modifyByBidx" parameterType = "boardVo">
		UPDATE boardtable
		SET title
		= #{title}
		, content = #{content}
		, hashTag = #{hashTag}
		WHERE bidx = #{bidx}
	</update>
</mapper>